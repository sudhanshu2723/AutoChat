

export default function Page() {
    // first onboard the user

    // 200 or 201 status code means the user is successfully onboarded
    // redirect to the dashboard page /{id} endpoint

    // if not redirect it to sign in endpoint
  return (
    <div>
       this is dashboard page 
    </div>
  )
}